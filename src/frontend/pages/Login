<!-- Tela de Login -->
 <!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login & Cadastro</title>
  <style>
    
    body { 
    
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg,#f57412, #f4ed34);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0;
    }
    .container {
      background: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      width: 100%;
      max-width: 350px;
      text-align: center;
      transition: 0.3s;
    }
    h2 {
      margin-bottom: 20px;
      color: #333;
    }
    input {
      width: 90%;
      padding: 10px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 5px;
      outline: none;
    }
    button {
      width: 100%;
      padding: 12px;
      background: #f57412;
      border: none;
      border-radius: 5px;
      color: #fff;
      font-size: 16px;
      cursor: pointer;
      margin-top: 10px;
    }
    button:hover {
      background:#f57412;
    }
    p {
      margin-top: 15px;
      font-size: 14px;
    }
    a {
      color: #f57412;
      text-decoration: none;
      cursor: pointer;
    }
    .hidden {
      display: none;
    }
  </style>
</head>
<body>
  <!-- Tela de Login -->
  <div class="container" id="login-container">
    <h2>Bem vindo ao</h2>
    <h2><span style="color: rgb(236, 115, 1);">ARREMATA</span></h2>

    <h2>Login</h2>
    <form>
      <input type="text" placeholder="Usuário" required>
      <input type="password" placeholder="Senha" required>
      <button type="submit">Entrar</button>
    </form>
    <p>Não tem conta? <a onclick="mostrarCadastro()">Cadastre-se</a></p>
  </div>

  <!-- Tela de Cadastro -->
  <div class="container hidden" id="cadastro-container">
    <h2>Cadastro</h2>
    <form>
      <input type="text" placeholder="Nome completo" required>
      <input type="email" placeholder="E-mail" required>
      <input type="text" id="cpf" name="cpf" maxlength="11" placeholder="Digite seu CPF" required>
      <input type="text" id="rg" name="rg" maxlength="12" placeholder="Digite seu RG" required>
      <input type="password" placeholder="Senha" required>
      <input type="password" placeholder="Confirmar senha" required>
      <button type="submit">Cadastrar</button>
    </form>
    <p>Já tem conta? <a onclick="mostrarLogin()">Faça login</a></p>
  </div>

  <script>
  function mostrarCadastro() {
    document.getElementById("login-container").classList.add("hidden");
    document.getElementById("cadastro-container").classList.remove("hidden");
  }

  function mostrarLogin() {
    document.getElementById("cadastro-container").classList.add("hidden");
    document.getElementById("login-container").classList.remove("hidden");
  }

  // Permitir apenas números no CPF
  document.getElementById("cpf").addEventListener("input", function() {
    this.value = this.value.replace(/\D/g, "");
  });

  // Permitir apenas números no RG
  document.getElementById("rg").addEventListener("input", function() {
    this.value = this.value.replace(/\D/g, "");
  });
</script>

</body>
</html>
